
\begin{frame}{An Ugly Example - With a Makeover}
    \begin{alltt}
\o{let} twice = $\Lambda$ \gr{eff}.(\\
~~~~$\lambda$ action: \bl{$\M{\texttt{\gr{eff}}}{\U}$}. (\\
~~~~~~~~\o{do} \_ <- action \o{in} action\\
~~~~)\\
)\\

do \_ <- (twice \gr{State} increment) \o{in} (twice \gr{IO} writeLog)
\end{alltt}
    \script{
        - We've now added polymorphism, which makes our ugly program more concise.
        - We can reuse this function
    }
\end{frame}